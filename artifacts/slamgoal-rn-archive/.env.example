# Base URL of the API server. Must be set for the mobile app to reach the API.
# In dev, point at your local api-server (e.g. http://localhost:3000 or an ngrok URL
# when testing on a physical device). In production/preview, EAS injects this
# automatically from eas.json.
EXPO_PUBLIC_API_URL=http://localhost:3000
